// export class chatMessage {
//   // 定义属性 sender，类型为 "User" 或 "AI"
//   sender: "User" | "AI";
//   // 定义属性 message，类型为字符串
//   message: string;
//
//   // 构造函数，用于初始化对象实例
//   constructor(sender: "User" | "AI", message: string) {
//     this.sender = sender; // 将传入的 sender 参数赋值给实例的 sender 属性
//     this.message = message; // 将传入的 message 参数赋值给实例的 message 属性
//   }
// }
//
//
//
